# Telegram bot token from @BotFather — used to validate Mini App initData.
BOT_TOKEN=

# HTTP/WS port.
PORT=8080

# Secret for signing your own session JWTs (issued after initData validation).
SESSION_SECRET=change-me

# Postgres / Redis (wire up in later build steps).
DATABASE_URL=
REDIS_URL=
